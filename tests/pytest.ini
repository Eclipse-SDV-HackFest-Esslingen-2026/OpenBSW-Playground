[pytest]
testpaths = tests
timeout = 30
markers =
    unit: CDA unit tests (no ECU, no network)
    ecu: ECU integration tests (requires ECU on TAP)
    sovd: SOVD API tests (requires ECU + stub CDA)
    grafana: Grafana visual rendering tests (requires Docker Compose stack)
