# opensovd-cda binary

The `opensovd-cda` executable is intentionally not stored in this repository.

Build it from source from the submodule
`OpenBSW-SOVD-Demo/real-sovd-cda/classic-diagnostic-adapter` and place
the produced binary here, or let the CI release pipeline build and
publish it as part of the release bundle.
